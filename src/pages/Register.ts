/**
 * Простая страница регистрации (заглушка).
 */
export function renderRegisterPage(appElement: HTMLElement): void {
  appElement.innerHTML = `
    <h2>Регистрация</h2>
    <p>Форма регистрации будет здесь.</p>
  `;
}
