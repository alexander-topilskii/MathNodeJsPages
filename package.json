{
  "name": "mathnodejspages",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && VITE_COMMIT_HASH=$(git rev-parse --short HEAD) VITE_COMMIT_DATE=$(git log -1 --format=\"%ai\") vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
